#include <iostream>

using namespace std;

int main()
{
    int n, a, k, exercise, errors;
    scanf("%d", &n);
    for(int i; i < n; i++){
        scanf("%d", a[i]);
    }
    for(int j; j < n; j++){
        for(int k; k < n; k++){
            if
        }
    }
    return 0;
}
